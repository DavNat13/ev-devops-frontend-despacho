name: Deploy Frontend Despacho

on:
  push:
    branches: ["deploy"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Configurar credenciales AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login en Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build y Push a ECR
        run: |
          docker build \
            --build-arg VITE_API_DESPACHOS_URL=http://${{ secrets.BACKEND_PRIVATE_IP }}:8081 \
            --build-arg VITE_API_VENTAS_URL=http://${{ secrets.BACKEND_PRIVATE_IP }}:8082 \
            -t frontend-despacho .
            
          docker tag frontend-despacho:latest ${{ secrets.ECR_REPO_URL_FRONTEND }}:latest
          docker push ${{ secrets.ECR_REPO_URL_FRONTEND }}:latest

      - name: Despliegue en EC2 vía SSM
        run: |
          aws ssm send-command \
            --instance-ids ${{ secrets.EC2_FRONTEND_INSTANCE_ID }} \
            --document-name "AWS-RunShellScript" \
            --comment "Actualizando Frontend" \
            --parameters '{"commands":[
              "cd /home/ec2-user/app",
              "echo ECR_REPO_URL_FRONTEND=${{ secrets.ECR_REPO_URL_FRONTEND }} > .env",
              "aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}",
              "docker compose pull frontend-despacho",
              "docker compose up -d frontend-despacho"
            ]}'
